pipeline {
    agent any

    stages {
        stage('print Choice parameter') {
            steps {
                echo "Hello ${MyFamily}"
            }
        }
         stage('print String parameter') {
            steps {
                echo "Your String parameter is: ${stringPara}"
            }
        }
    }
}
